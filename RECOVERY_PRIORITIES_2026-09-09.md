# Recovery Priorities - Wednesday 2026-09-09

Last valid quiz: Daily Quick, 10/12 (83.3%).

## P1 - Must Appear Early

- `provision` vs `protection`: The learner selected `protection` for a
  contingency arrangement in a continuity plan. A plan or contract contains a
  `provision`; equipment provides `protection`.
- `profound` vs `profoundly`: The learner selected adjective `profound` where
  the blank modified the verb `changed`. Use `profound change` but
  `profoundly changed`.
- `semiconscious` vs `subconscious`: The physical-state answer was correct
  confidently after repeated errors. Retain P1 for one more new-context check
  before lowering it.
- `substitution` vs `subtitle` / `substitute`: The replacement-action answer
  was correct confidently after the latest `subtitle` error. Retain P1 for one
  more check.

## P2 - Include Soon

- `protection` remains inside the provision/protection noun-role contrast.
  `protective` moves to cooldown after another confident adjective use.
- `subtitle` stays on light Recovery watch with `substitution`.
- `semicircle` vs `semicircular`, `tolerating` vs `endurance`, and
  `returnable` vs `renewable` remain in the existing recovery cycle.
- Continue untested Unit 35 words after confident first practice of `submit`,
  `submerge`, and `subway`.
- Continue Unit 36 `super-` coverage, especially `superficial` /
  `superficially` / `superficiality` and `supervise` / `supervisor` after
  `supervision` was correct.
- Begin Unit 37 `syn-` / `sym-` at P2. Prioritize `synchronize` /
  `synchronization`, `synonym` / `synonymous`, `sympathy` / `sympathetic` /
  `sympathize`, `symphony` / `symphonic`, and `syndrome` / `symptom`.

## Cooldown / Light Watch

- `protective`: correct confidently before `foam` after several stable
  adjective uses.
- `perceptive` vs `observable`: `perceptive` was correct for a person on
  2026-09-08 and `observable` was correct for a measurable change today.
- `outburst`: correct confidently for a sudden angry expression.
- `threshold` vs `ratio`, `semiannual`, `objective`, and `midstream` remain
  on cooldown.

## Next Quiz

- Thursday 2026-09-10: Daily Quick, 12 questions.
