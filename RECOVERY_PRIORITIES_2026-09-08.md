# Recovery Priorities - Tuesday 2026-09-08

Last valid quiz: Daily Quick, 10/12 (83.3%).

## P1 - Must Appear Early

- `provision` vs `protection` / `protective`: `provision` was correct in a vendor-contract clause this time, but it remains P1 after several recent noun-role errors. A contract contains a `provision`; a case offers `protection`; `protective` describes the case.
- `profound` vs `profoundly`: `profound` was correct before noun `influence`. Keep one more early adjective/adverb retrieval before lowering its priority.
- `semiconscious` vs `subconscious`: The learner again chose `subconscious` for someone partly awake after an accident. `semiconscious` is a physical state; `subconscious` is a part of the mind below awareness.
- `substitution` vs `subtitle` / `substitute`: The learner chose `subtitle` for replacing an original headline. `substitution` is the replacement action; a `subtitle` is text on screen; a `substitute` is the person or thing used instead.

## P2 - Include Soon

- `protection` inside the protect/provide family: correct twice in recent physical-safety noun contexts; keep a later check while `provision` remains P1.
- `perceptive` vs `observable`: `perceptive` was correct for a person who notices a pattern. Recheck later against an `observable` result or change.
- `subtitle`: include with `substitution` to stabilize the new contrast.
- `semicircle` vs `semicircular`, `tolerating` vs `endurance`, and `returnable` vs `renewable`: retain the existing recovery cycle; none was tested today.
- New Unit 35 coverage: `subtotal` and `substandard` were correct; continue untested `sub-` words in realistic contexts.
- New Unit 36 `super-` coverage: begin with the book words and word-family role
  contrasts `superficial` / `superficially` / `superficiality` and
  `supervise` / `supervision` / `supervisor`.

## Cooldown / Light Watch

- `threshold` vs `ratio`: `threshold` was correct confidently for a 2% trigger level. Check only occasionally.
- `semiannual`: correct confidently for June-and-December reviews.
- `objective`: correct confidently for an unbiased reviewer.
- `midstream`: correct confidently for changing requirements during an ongoing process.

## Next Quiz

- Wednesday 2026-09-09: Daily Quick, 12 questions.
