# Word Formation Addendum - Units 22-29

This file records the latest known vocabulary units from the project state.
Merge it into the full word-formation source if that source is available.

## Unit 22 - `mid-`

Meaning: middle, halfway, during the middle of a time/place/process.

Words:

- `midday`
- `midlife`
- `midnight`
- `midterm`
- `midtown`
- `midway`
- `mid-air`
- `midstream`
- `midwinter`
- `midpoint`
- `mid-range`
- `midweek`
- `mid-afternoon`
- `mid-century`
- `midsize`

Watch:

- `midstream` = in the middle of a process, not only streaming data.

## Unit 23 - `mis-`

Meaning: wrong, mistaken, badly, incorrectly.

Book words:

- `misbehave`
- `mislead`
- `misunderstand`
- `misuse`
- `mistake`

Extended words:

- `mistreat`
- `misread`
- `misspell`
- `mismatch`
- `miscount`
- `mistrust`
- `mishear`
- `mishandle`

Supplement:

- `drug`

Watch:

- `misuse data` = use data in the wrong way or for the wrong purpose.
- `mislead customers` = make customers believe something false.
- `mismatch` = things do not match.
- `mishandle an incident` = handle/manage an incident badly.
- `misbehave` = behave badly.

## Unit 24 - `multi-`

Meaning: many, multiple, many parts/functions.

Book words:

- `multicultural`
- `multiple`
- `multiply`
- `multipurpose`
- `multitask`

Extended words:

- `multimedia`
- `multinational`
- `multilingual`
- `multicolor`
- `multilayer`
- `multiplayer`
- `multi-author`
- `multi-skilled`
- `multistory`
- `multi-access`

Watch:

- `multipurpose tool` = a tool with many uses.

## Unit 25 - `non-`

Meaning: not, without, outside the stated group or state.

Book words:

- `nonfiction`
- `nonprofit`
- `nonsense`
- `nonstop`
- `nonverbal`

Extended words:

- `non-smoking`
- `non-fat`
- `non-human`
- `non-violent`
- `non-native`
- `non-essential`
- `non-plastic`
- `non-swimmer`
- `non-urgent`
- `non-scented`

Watch:

- `non-urgent` = not needing immediate action.
- `non-essential` = not necessary for the main/core function.

## Unit 26 - `ob-` / `op-` / related `ob` family

Book words:

- `object` = disagree; express opposition
- `obliged` = required; forced by duty or situation
- `observe` = watch carefully; notice
- `obstacle` = barrier; thing blocking progress
- `obvious` = clear; easy to see

Family words:

- `objective`
- `objection`
- `oblige`
- `obligatory`
- `obligation`
- `observable`
- `observant`
- `observation`
- `observatory`
- `obviously`
- `obviousness`

Extended words:

- `obey`
- `obtain`
- `obsess`
- `object`
- `obstruct`
- `oppose`
- `offend`
- `oppress`

Watch:

- `objective` = fair, factual, unbiased.
- `observant` = good at noticing details.
- `observable` = can be seen or measured.
- `obstruct` = block or prevent progress.
- `obstacle` = a barrier or difficulty.

## Unit 27 - `out-`

Meaning: out, outside, beyond, exceeding, surpassing.

Book words:

- `outbreak` = sudden start or spread, often disease/violence
- `output` = amount produced; production result
- `outside` = not inside
- `outsource` = hire an external company/person to do work
- `outweigh` = be greater or more important than

Extended words:

- `outdated` = no longer modern or useful
- `outdoor` = outside, in the open air
- `outfit` = set of clothes
- `outline` = summary structure; sketch main points
- `outbound` = going out or away
- `outskirts` = outer area of a city/town
- `outgrow` = become too big/mature for something
- `outward` = toward the outside; external
- `outburst` = sudden expression of strong emotion
- `outspoken` = direct and honest in speech

Extra words added with Unit 27:

- `boredom` = state of being bored
- `assume` = suppose something is true without full proof

Watch:

- `outbreak` vs `outburst`: disease/event spread vs emotion.
- `outspoken` = person who says opinions directly.
- `outgrow` often appears with clothes, habits, apartments, old systems.

## Unit 28 - `over-`

Meaning: too much, above, more than, beyond a limit.

Book words:

- `overestimate` = estimate too high
- `overload` = put too much load on; overwhelm a system
- `overreact` = react too strongly
- `overhead` = above one's head; business operating costs
- `overweight` = heavier than is healthy/expected

Extended words:

- `oversize` = too large
- `overcharge` = charge too much money
- `overlook` = fail to notice; also look over/from above
- `overwork` = work too much
- `oversleep` = sleep too long; wake late
- `overwhelmed` = feeling unable to cope because of too much pressure
- `overhear` = hear accidentally
- `overcook` = cook too much
- `overuse` = use too much
- `overnight` = during the night; very quickly
- `overspend` = spend too much
- `overpriced` = too expensive
- `overjoyed` = extremely happy
- `overeat` = eat too much
- `overpay` = pay too much

Watch:

- `overestimate` vs `underestimate`.
- `overlook` can mean "fail to notice"; do not only use "look over".
- `oversleep` is triggered by alarms, late arrival, or waking too late.
- `overheard` is the past form of `overhear`.

## Unit 29 - `per-`

Current learning focus: important `per-` vocabulary and word families.

Book words:

- `perceive` = become aware of; understand through senses or mind
- `perfect` = without mistakes; excellent
- `persevere` = continue despite difficulty, usually positive
- `persist` = continue; may be positive or negative depending on context
- `persuade` = convince someone to do or believe something

Word families:

`perceive`:

- `perceive`
- `perception`
- `perceptive`

`perfect`:

- `perfect`
- `perfectly`
- `perfection`
- `perfectionism`
- `imperfect`

`persevere`:

- `persevere`
- `perseverance`
- `persevering`

`persist`:

- `persist`
- `persistence`
- `persistent`
- `persistently`

`persuade`:

- `persuade`
- `persuasion`
- `persuasive`

Extended words:

- `permanent` = lasting; not temporary
- `perform` = carry out; act; do
- `permit` = allow
- `perspective` = point of view
- `perfume` = pleasant smell or scented liquid
- `pervade` = spread through and be present everywhere in something

Additional Outwork vocabulary:

- `symptoms` = signs of illness or a problem
- `boredom tolerance` = ability to endure boredom
- `monotonous` = repetitive and dull
- `tap out` = quit; give up
- `stimulation` = mental/physical excitement or activation

Watch:

- `persist` can be neutral/negative: a problem persists.
- `persevere` is usually positive: a person perseveres through difficulty.
- `perception` is how something is understood; `perspective` is point of view.
- `persuasive` describes something/someone that convinces.
- `perceptive` describes someone who notices or understands quickly.
- `monotonous` describes repetitive and dull work; `perceptive` describes a person, not a repetitive task.



endurance : sức bền 
tolerating : chịu đựng 
