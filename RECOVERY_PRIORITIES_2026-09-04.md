# Recovery Priorities - Friday 2026-09-04

Last valid quiz: Weekly Quick, 23/30 (76.7%).

## P1 - Must Appear Early

- `provision` vs `protective` / `protection`: `provision` is a contract clause or future arrangement; `protection` is the noun after phrases like `a layer of`; `protective` is the adjective before a noun.

## P2 - Include Soon

- `semicircle` vs `semicircular`: both forms were correct today, so reduce from P1 but retain Recovery after recent repeated word-form errors.
- `tolerating` vs `endurance`: keep a later check after the correct Weekly Quick answer.
- `returnable` vs `renewable`: keep a later check after the correct Weekly Quick answer.
- `suburb` vs `suburban`: a `suburb` is a place; `suburban` describes a neighborhood or lifestyle.
- `proceed` vs `progress`: `proceed` means continue with an action; `progress` means improvement or movement forward.
- `rewritable` vs `renewable`: a whiteboard is `rewritable`; a resource that replaces itself naturally is `renewable`.
- `profound` vs `profoundly`: `profound` is an adjective before a noun such as `effect`; `profoundly` is an adverb.
- `perceptive` vs `observable`: a person who notices details is `perceptive`; something that can be seen or measured is `observable`.

## Current Unit Coverage

- Continue Unit 35 `sub-` with `subconscious`, `substandard`, `substitute` / `substitution`, `subtitle` / `subheading`, and less-seen extended words.
- Continue the due Unit 30-34 rotation without repeating the exact Friday sentences.

## Light Watch

- `proactive`: correct confidently; move to cooldown.
- `semi-permanent`, `semisweet`, `post-traumatic`, `preorder` / `pre-book`, `post-flight`, `outbreak`, `objective`, `non-urgent`, `midstream`, and `ratio`: correct in the Weekly Quick.

## Next Quiz

- Monday 2026-09-07: Daily Quick, 12 questions.
