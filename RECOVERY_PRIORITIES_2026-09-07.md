# Recovery Priorities - Monday 2026-09-07

Last valid quiz: Daily Quick, 8/12 (66.7%).

## P1 - Must Appear Early

- `provision` vs `protection` / `protective`: The learner selected `protection` for a service agreement clause. `provision` is the arrangement or clause; `protection` is safety as a noun; `protective` is the adjective before a noun.
- `profound` vs `profoundly`: The learner again selected adverb `profoundly` before noun `effect`. `profound` is the adjective in `a profound effect`; `profoundly` modifies verbs or adjectives.

## P2 - Include Soon

- `semiconscious` vs `subconscious`: The learner selected `subconscious` for a person who was partly awake after fainting. `semiconscious` means partly conscious; `subconscious` means below awareness in the mind.
- `threshold` vs `ratio`: The learner selected `ratio` for `pass the 5% threshold`. `threshold` is a trigger level; `ratio` is a relationship between two amounts.
- `substitution`: Correct but uncertain. Keep light watch and reinforce that it means replacement, not only a general late change.
- `protection` within the protect family: Correct after `a layer of`, but keep near recovery because `provision` remains unstable.
- `semicircle` vs `semicircular`: Retain light recovery after recent repeated word-form errors, although the last Weekly Quick answers were correct.
- `perceptive` vs `observable`: Keep a later check after the Friday miss.
- `tolerating` vs `endurance`: Keep a later check after recent improvement.
- `returnable` vs `renewable`: Keep a later check after recent improvement.

## Cooldown / Light Watch

- `proceed` vs `progress`: Correct confidently on 2026-09-07 after prior unresolved answer.
- `rewritable` vs `renewable`: Correct confidently on 2026-09-07 after prior miss.
- `suburb` vs `suburban`: `suburban` correct confidently on 2026-09-07 after prior miss.
- `subconscious`: Correct in hidden-fear context but contrast with `semiconscious` now needs reinforcement.
- `subheading`: Correct.
- `illegible` vs `illiterate`: Correct in handwriting context.

## Next Quiz

- Tuesday 2026-09-08: Daily Quick, 12 questions.
